class Solution {
public:
    int maxProfit(vector<int>& arr) {
        int ans = 0;
        int buy = arr[0];
        
        for(int i = 1; i < arr.size(); i++)
        {
            ans = max(ans, arr[i] - buy);
            buy = min(buy, arr[i]);
        }
        
        return ans;
    }
};
